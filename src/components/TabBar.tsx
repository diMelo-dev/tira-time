import { Text, View } from 'react-native'

type TabbarProps = {
  currentTab?: string
}

export const Tabbar = (props: TabbarProps) => {
  
  return (
    <View>
      <Text>Elemento de texto</Text>
    </View>
  )
}